# URL base da API REST do backend (Node.js/Express), sem barra final.
API_BASE_URL=http://localhost:3333/api

# Nome do cookie httpOnly usado para armazenar o JWT do admin.
AUTH_COOKIE_NAME=sav_token
